    <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Gotta Catch 'Em All</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            
        {% if current_user.is_anonymous %}
            <a class="nav-link" href="{{url_for('auth.login')}}">Login</a>
            <a class="nav-link" href="{{url_for('auth.register')}}">Register</a>
        {% endif%}

        {%if current_user.is_authenticated%}
            <a class="nav-link" href="{{url_for('auth.index')}}">Home</a>
            <a class="nav-link" href="{{url_for('main.my_team')}}">My Team</a>
            <a class="nav-link" href="{{url_for('main.poke_finder')}}">Catch Pokemon</a>
            <a class="nav-link" href="{{url_for('auth.edit_profile')}}">Edit Profile</a>
            <a class="nav-link" href="{{url_for('social.show_users')}}">Discover</a>            
            <a class="nav-link" href="{{url_for('auth.logout')}}">Logout</a>
            
            
        {% endif%}                           
                      
        </div>

        {% if current_user.is_authenticated %}
        <ul class="navbar-nav ms-auto align-items-center">
            <img src="{{current_user.get_icon_url()}}" style="height:50px;">
            </a>
            <li class="nav-item">
                <div class="row">
                     <div class="col-12">
                        <a href="{{url_for('auth.edit_profile')}}">
                            <i class="bi bi-gear-fill"></i>
                        </a>
                      </div>
                      <div class="col-12">
                         <a href="{{url_for('auth.logout')}}">
                             <i class="bi bi-box-arrow-right"></i>
                         </a>
                      </div>
                </div>
            </li>
        </ul>
        {% endif %}
        
        

       
        </div>
    </div>    
    </nav>